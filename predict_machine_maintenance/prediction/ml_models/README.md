Place your trained model and scaler here after running `train_model.py`.

Expected files:
- fault_detection_model.pkl
- scaler.pkl

You can keep `feature_order.json` in the project root to map dict inputs to ordered feature vectors.
